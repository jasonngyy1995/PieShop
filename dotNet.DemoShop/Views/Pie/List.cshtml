<!-- A Razor View which is one on one mapping between
the name of the action method and the name of view that
will get returned--> 

<!-- the model for this view -->
@model PiesListViewModel


@foreach (var pie in Model.Pies)
{
    <partial name="_PieCard" model="pie" />
}
